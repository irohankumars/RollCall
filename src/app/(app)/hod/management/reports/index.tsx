import { router, type Href } from 'expo-router';
import { PageContainer } from '@/shell/app-shell';
import { HodShell } from '@/hod/components';
import { ManagementHeader, ManagementRow, RelationshipSection } from '@/hod/management-components';

const reports=[['student','Student attendance','Student status, class, and attendance percentage'],['subject','Subject attendance','Subject coverage and conducted-session participation'],['class','Class / section attendance','Class completion and student attention'],['semester','Semester attendance','Semester-level attendance summary'],['batch','Batch attendance','Batch participation and attention'],['department','Department attendance','Department completion and overall attendance'],['lecturer','Lecturer / class activity','Teaching assignments and session completion']] as const;
export default function ReportsLanding(){return <HodShell activeKey="department" title="Reports" subtitle="Department reporting" back backFallback="/hod/management"><PageContainer width="detail"><ManagementHeader title="Reports" description="Focused department views with only the filters relevant to each report." /><RelationshipSection title="Attendance reports">{reports.slice(0,6).map(([id,title,detail])=><ManagementRow key={id} title={title} detail={detail} onPress={()=>router.push(`/hod/management/reports/${id}` as Href)} />)}</RelationshipSection><RelationshipSection title="Activity reports">{reports.slice(6).map(([id,title,detail])=><ManagementRow key={id} title={title} detail={detail} onPress={()=>router.push(`/hod/management/reports/${id}` as Href)} />)}</RelationshipSection></PageContainer></HodShell>;}
